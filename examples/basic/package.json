{
  "name": "@tsuzuru/example-basic",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @tsuzuru/core build && tsc -p tsconfig.json",
    "start": "pnpm build && node dist/run.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@tsuzuru/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
