# Tsuzuru v1.0.0 Release Notes

> Status: npm packages published; GitHub release pending
> This document records the npm publish and post-publish smoke result. The git
> tag and GitHub release are still pending.

## Release Summary

Tsuzuru v1.0.0 stabilizes the current web-first visual novel engine foundation
around the modern `.tzr` DSL.

This document covers the npm-published release state before the git tag and
GitHub release are created. It records the manual package version bump, local
release gates, publish dry-runs, actual npm publish completion, and
post-publish registry smoke result.

Target release version: `1.0.0`.

The current repository inventory and all npm publish targets are `1.0.0`. The
14 npm packages listed below have been published, and the post-publish registry
smoke has passed. The git tag and GitHub release remain pending maintainer
steps.

## Release Date

2026-05-17.

This release date records the npm publish and post-publish smoke completion
date. The git tag and GitHub release are still pending.

## Package List

Publish targets match the v1.0 manual publish checklist. The packages are
listed in publish order.

| Package | Directory | Current inventory version | Release target | Notes |
| --- | --- | --- | --- | --- |
| `@tsuzuru/core` | `packages/core` | `1.0.0` | `1.0.0` | Parser, compiler, project compiler, runtime, snapshots, save-slot validation, and plugin command infrastructure. |
| `@tsuzuru/config` | `packages/config` | `1.0.0` | `1.0.0` | Project configuration helpers. |
| `@tsuzuru/vite-plugin` | `packages/vite-plugin` | `1.0.0` | `1.0.0` | Vite plugin for importing `.tzr` scenarios. |
| `@tsuzuru/plugin-std-visual` | `packages/plugin-std-visual` | `1.0.0` | `1.0.0` | Standard visual plugin state and command handlers. |
| `@tsuzuru/plugin-std-audio` | `packages/plugin-std-audio` | `1.0.0` | `1.0.0` | Standard audio plugin state, command handlers, and save-ready one-shot cleanup helper. |
| `@tsuzuru/plugin-std-text-sound` | `packages/plugin-std-text-sound` | `1.0.0` | `1.0.0` | Standard text sound plugin and `/browser` helper subpath. |
| `@tsuzuru/plugin-std-effect` | `packages/plugin-std-effect` | `1.0.0` | `1.0.0` | Standard one-shot effect plugin and save-ready cleanup helper. |
| `@tsuzuru/plugin-std-camera` | `packages/plugin-std-camera` | `1.0.0` | `1.0.0` | Durable camera state plugin. |
| `@tsuzuru/plugin-std-particle` | `packages/plugin-std-particle` | `1.0.0` | `1.0.0` | Durable particle state plugin. |
| `@tsuzuru/plugin-std-system` | `packages/plugin-std-system` | `1.0.0` | `1.0.0` | Durable unlock state plugin for endings, CGs, and achievements. |
| `@tsuzuru/preact` | `packages/preact` | `1.0.0` | `1.0.0` | Preact runtime adapter and save/load helper utilities. |
| `@tsuzuru/standard-ui-preact` | `packages/standard-ui-preact` | `1.0.0` | `1.0.0` | Reusable Preact UI components and `./style.css` export. |
| `@tsuzuru/cli` | `packages/cli` | `1.0.0` | `1.0.0` | `tsuzuru` CLI, including `tsuzuru check`. |
| `create-tsuzuru` | `packages/create-tsuzuru` | `1.0.0` | `1.0.0` | Project generator. Publishes after the `@tsuzuru/*` packages because generated templates depend on published package versions. |

Not publish targets:

- root `tsuzuru`, which is `private: true`
- `examples/preact-basic`, which is `private: true`
- `examples/preact-starter`, which is `private: true`
- repository docs, tests, plans, workflows, and local tooling

## Highlights

- Modern `.tzr` DSL v2 is the current scenario authoring path. The public parser
  and compiler APIs are `parseTzr` and `compileTzr` from `@tsuzuru/core`.
- `@tsuzuru/core` provides the browser-independent parser, compiler, project
  compiler, runtime IR, runtime stepping, choices, jumps, conditional branches,
  scenario state updates, plugin command dispatch, snapshots, and save-slot
  validation.
- Include-based multi-file scenario projects are supported through
  `include "./path.tzr"` and `compileTzrProject`.
- `@tsuzuru/cli` provides `tsuzuru check` for scenario validation through the
  configured project entry and scenario file set.
- `@tsuzuru/vite-plugin` supports importing `.tzr` scenario files in Vite
  projects.
- `create-tsuzuru` provides the current project generator path for the basic
  Preact template.
- `@tsuzuru/preact` connects the core runtime to framework applications without
  moving scenario semantics out of core.
- `@tsuzuru/standard-ui-preact` provides reusable Preact visual novel UI
  components, including message, choice, viewport, and text reveal helpers.
- Official standard plugins cover visual, audio, text sound, effect, camera,
  particle, and system unlock state through runtime command handlers.
- Save/load validation is layered through `RuntimeSnapshot.version === 2`,
  `RuntimeSaveSlot.version === 1`, and adapter `RuntimeSaveData.version === 2`.
- The Preact example includes RuntimeSaveSlot-based Save -> Load -> Restore
  coverage, with browser E2E available as an optional workflow.
- Release-readiness and publish-readiness gates check inventory, package
  artifacts, packed package contents, examples, and local `create-tsuzuru`
  smoke before an actual release is approved.

## Breaking Changes

For users coming from earlier experimental v0.x builds, v1.0 should be treated
as the stabilization point for the current DSL and runtime boundary.

- Legacy DSL syntax is not supported:
  - `#scene(...)`
  - `#label(...)`
  - `:: Speaker`
  - `@command(...)`
  - `$macro(...)`
  - legacy `@if` / `@else` / `@endif`
- Transitional parser/compiler names are not current APIs. Use `parseTzr` and
  `compileTzr`; do not use removed `parseTzrV2` or `compileTzrV2` aliases.
- Macro support, scenario-local macro definitions, preset syntax, stage syntax,
  and arbitrary JavaScript or TypeScript inside `.tzr` files are not part of
  v1.0.
- Parser-only or design-only syntax is not included in the v1.0 stable DSL
  subset. This includes rich inline text, inline waits/events, blank-line click
  waits, `---` page breaks, text block `:meta`, `system.*` condition reads,
  visual coordinate placement, and audio transition syntax.
- `@tsuzuru/core` remains independent from DOM, Preact, Vue, CSS, Vite,
  browser storage, and application asset loading. Hosts and framework adapters
  own presentation and storage policy.

## Migration Notes

- Replace old DSL syntax with the current DSL v2 syntax. Use the current docs
  and examples, especially `examples/preact-basic`, as the reference.
- Import parser/compiler APIs from `@tsuzuru/core` as `parseTzr`,
  `compileTzr`, and, for multi-file projects, `compileTzrProject`.
- Use `include "./path.tzr"` for multi-file scenario authoring. The include
  directive is compile-time only and does not become a runtime event.
- Register official standard plugin definitions when compiling scenarios that
  use standard plugin sugar or `call namespace.command(...)` validation.
- Do not rely on save data migration. v1.0 validates current save formats and
  rejects unsupported versions or mismatched scenario identity/version data.
- Scenario edits may invalidate existing save data because compiled instruction
  order, scene shape, runtime state, runtime events, and scenario version can
  change.
- `RuntimeSnapshot` and `RuntimeSaveSlot` validation rejects malformed, old,
  future, scenario-mismatched, and invalid nested snapshot payloads according to
  the current validation boundary.
- Plugin state migration is not guaranteed. Durable standard plugin state is
  preserved as runtime plugin state, while one-shot audio/effect events require
  the documented prepare helpers before creating save-ready snapshots.
- Generated projects should be regenerated or manually aligned when upgrading
  templates. `create-tsuzuru` templates are not a save-data or app-code
  migration framework.

## Known Limitations

- Tsuzuru v1.0 does not provide a save data migration framework for
  `RuntimeSnapshot`, `RuntimeSaveSlot`, adapter `RuntimeSaveData`, plugin state,
  scenario versions, host localStorage, IndexedDB, or remote storage.
- User-facing invalid save slot reason UI is minimal and example-level.
  Examples may filter invalid slots instead of showing detailed recovery
  reasons.
- Browser E2E remains optional. `pnpm examples:e2e` verifies the Preact browser
  flow, including Save -> Load -> Restore coverage, but it is not part
  of root `test` or `release-readiness:check`.
- Package graph validation remains optional/manual for v1.0. The experimental
  `packages:graph:check` is not the required distribution gate.
- Editor tooling, syntax highlighting, VS Code extension support, LSP
  diagnostics, and GUI editor support are not part of the v1.0 engine gate.
- Audio transitions such as `bgm ... with fadeIn(...)` and
  `stopBgm with fadeOut(...)` remain post-v1.0 design work.
- Visual coordinate placement such as `show asset at x=... y=...` remains
  post-v1.0 renderer coordinate policy work.
- Rich text, inline runtime events, inline audio events, blank-line click waits,
  page breaks, and text block metadata remain post-v1.0 text model work.
- `system.*` condition reads remain post-v1.0 resolver design work. The current
  std-system write side uses plugin commands such as
  `call system.unlockEnding(...)`.
- The git tag and GitHub release creation remain pending release administration
  steps after npm package publication.

## Verification Summary

Final release verification was run from the manually bumped `1.0.0` inventory
before actual npm publication. The release blocker fix was verified on
2026-05-17 before the packages were published.

| Command | Required for v1.0 release gate | Status |
| --- | --- | --- |
| `pnpm inventory:check` | Yes | Passed on 2026-05-17 before npm publish |
| `pnpm format:check` | Yes | Passed on 2026-05-17 before npm publish |
| `pnpm lint` | Yes | Passed on 2026-05-17 before npm publish |
| `pnpm check` | Yes | Passed on 2026-05-17 before npm publish |
| `pnpm test` | Yes | Passed on 2026-05-17 before npm publish |
| `pnpm typecheck` | Yes | Passed on 2026-05-17 before npm publish |
| `pnpm packages:build` | Yes | Passed on 2026-05-17 before npm publish |
| `pnpm publish-readiness:check` | Yes | Passed on 2026-05-17 before npm publish |
| `pnpm release-readiness:check` | Yes | Passed on 2026-05-17 before npm publish |
| `git diff --check` | Yes | Passed on 2026-05-17 before npm publish |

`release-readiness:check` is the distribution gate. It runs package builds,
example self-checks, pack dry-run, publish-readiness, and local
`create-tsuzuru` smoke.

## Optional Verification

These checks are useful confidence checks but are not required v1.0 release
gates today.

| Command | Status | Notes |
| --- | --- | --- |
| `pnpm examples:e2e` | Passed on 2026-05-17 after message window layout stabilization | Preact: 11 passed. |
| `pnpm packages:graph:check` | Passed on 2026-05-17 release preparation run | Experimental TypeScript package graph validation, not distribution validation. |

## Publish Dry-run Verification

Publish dry-runs were completed before actual npm publish with
`pnpm --filter <package> publish --dry-run --no-git-checks`. The
`--no-git-checks` flag was used only because this release-preparation run
intentionally performs dry-runs before creating the preparation commit. Every
publish command still used `--dry-run` and reported skipped publishing.

| Package | Version | Dry-run status |
| --- | --- | --- |
| `@tsuzuru/core` | `1.0.0` | Passed on 2026-05-17 |
| `@tsuzuru/config` | `1.0.0` | Passed on 2026-05-17 |
| `@tsuzuru/vite-plugin` | `1.0.0` | Passed on 2026-05-17 |
| `@tsuzuru/plugin-std-visual` | `1.0.0` | Passed on 2026-05-17 |
| `@tsuzuru/plugin-std-audio` | `1.0.0` | Passed on 2026-05-17 |
| `@tsuzuru/plugin-std-text-sound` | `1.0.0` | Passed on 2026-05-17 |
| `@tsuzuru/plugin-std-effect` | `1.0.0` | Passed on 2026-05-17 |
| `@tsuzuru/plugin-std-camera` | `1.0.0` | Passed on 2026-05-17 |
| `@tsuzuru/plugin-std-particle` | `1.0.0` | Passed on 2026-05-17 |
| `@tsuzuru/plugin-std-system` | `1.0.0` | Passed on 2026-05-17 |
| `@tsuzuru/preact` | `1.0.0` | Passed on 2026-05-17 |
| `@tsuzuru/standard-ui-preact` | `1.0.0` | Passed on 2026-05-17 |
| `@tsuzuru/cli` | `1.0.0` | Passed on 2026-05-17 |
| `create-tsuzuru` | `1.0.0` | Passed on 2026-05-17 |

Workspace protocol dependencies were checked through a companion temporary pack
manifest inspection after the publish dry-runs. All 14 packed manifests were
`1.0.0`, no `workspace:` protocol remained, and internal `@tsuzuru/*`
dependencies were converted to `^1.0.0`.

## npm Publish Record

The maintainer reports that actual npm publish completed on 2026-05-17 for all
14 publish targets listed in this document. Published package versions are
`1.0.0`.

The git tag and GitHub release have not been created yet.

## Post-publish Verification

Post-publish verification completed after all npm packages were published.

| Command | Status | Result |
| --- | --- | --- |
| `pnpm run smoke:create-tsuzuru:registry` | Passed on 2026-05-17 | Passed on 2026-05-17 after all packages were published |

This registry smoke verifies that the published `create-tsuzuru` package can
generate a project that installs from the registry, validates its scenario, and
builds.

## Related Release Gate Documents

- [v1.0 Release Gate](../plans/v1.0-release-gate.md)
- [v1.0 Manual Publish Checklist](../plans/v1.0-manual-publish-checklist.md)
- [DSL Support Matrix](../design/dsl-support-matrix.md)
- [ADR 0031: v1.0 Save / Load Compatibility Promise](../decisions/0031-v1-save-load-compatibility-promise.md)
- [v1.0 Standard Plugin Save/Load Matrix](../plans/v1.0-standard-plugin-save-load-matrix.md)

## Maintainer Checklist

- [x] Confirm this document says `Status: npm packages published; GitHub
      release pending`.
- [x] Confirm the package list matches the manual publish checklist.
- [x] Confirm all package versions are `1.0.0`.
- [x] Confirm final release verification passed before npm publish.
- [x] Confirm package publish completed for all 14 publish targets.
- [x] Confirm post-publish registry smoke passed with
      `pnpm run smoke:create-tsuzuru:registry`.
- [ ] Create the annotated git tag.
- [ ] Push `main` and the `v1.0.0` tag.
- [ ] Create the GitHub release from this release notes file.

## Notes for GitHub Release

Use this document as the source for the GitHub release notes. The npm packages
and post-publish registry smoke are complete; the git tag and GitHub release are
the remaining release administration steps.

Before creating the GitHub release:

- confirm the `v1.0.0` annotated tag has been created and pushed
- confirm `main` contains the release preparation, blocker fix, and release
  notes finalization commits
- use `docs/releases/v1.0.0.md` as the GitHub release notes file
- do not describe the GitHub release as completed until `gh release create` has
  actually run
