# v0.10 Save / Load MVP Plan

Status: implemented in `examples/preact-basic`.

## Goal

Add the minimum save/load flow needed for the runnable example to persist and
restore runtime progress.

The goal is to validate the existing `RuntimeSaveData` restore path in a real
host application. It is not to settle Tsuzuru's long-term storage adapter
design.

## Scope

- localStorage-backed save slots in `examples/preact-basic`
- three fixed save slots
- Title Continue from the latest save slot
- title-screen Load from a selected slot
- runtime Save and Load overlays that do not unmount `RuntimeApp`
- defensive validation of persisted data with `isRuntimeSaveData`
- example-level scenario identity and scenario version in save slot payloads
- migration from earlier example payloads and raw `RuntimeSaveData` payloads
- Playwright smoke coverage for Continue and basic save/restore

## Non-goals

- changes to `@tsuzuru/core`
- changes to `@tsuzuru/preact`
- changes to `@tsuzuru/standard-ui-preact`
- IndexedDB
- cloud save
- file save / Electron save
- screenshot thumbnails
- quick save / quick load
- auto save
- `create-tsuzuru` template propagation
- a standard UI SaveLoadPanel

## Implemented files

- `examples/preact-basic/src/game-storage.ts`
- `examples/preact-basic/src/screens/SaveScreen.tsx`
- `examples/preact-basic/src/screens/LoadScreen.tsx`
- `examples/preact-basic/src/screens/TitleScreen.tsx`
- `examples/preact-basic/src/App.tsx`
- `examples/preact-basic/src/style.css`
- `examples/preact-basic/tests/save-storage.test.ts`
- `examples/preact-basic/tests/visual.spec.ts`

## Save data identity

The original MVP save slot payloads used `ExampleSaveData.version: 2`. The
current example save wrapper is `ExampleSaveData.version: 3`, while compatible
v2 payloads remain migratable. The v2 payload included the example scenario
identity:

```ts
{
  id: "tsuzuru.example.preact-basic",
  version: "1"
}
```

The localStorage key remains `tsuzuru:example-preact-basic:saves:v1` so existing
local saves can still be read. `version: 1` example save data and legacy raw
`RuntimeSaveData` payloads do not contain scenario identity, so the MVP migrates
them as data for the current example scenario and attaches the current identity.
`version: 2` payloads with a different scenario id or scenario version are not
exposed as load slots and are not considered by Title Continue.

This is intentionally scoped to the browser localStorage example. It is not a
formal Tsuzuru storage adapter, migration framework, or config schema.

## Verification

The implementation was verified with:

```sh
rtk pnpm --filter @tsuzuru/example-preact-basic typecheck
rtk pnpm --filter @tsuzuru/example-preact-basic build
rtk pnpm --filter @tsuzuru/example-preact-basic test
rtk pnpm --filter @tsuzuru/example-preact-basic test:ui
rtk git diff --check
```

The UI smoke test clears the example localStorage key before the run, verifies
that Continue is initially disabled, saves Slot 1 during runtime, returns to the
title screen, confirms Continue is enabled, and restores into the message
window.

## Follow-up tasks

- Design a formal save data migration framework.
- Decide how screenshot thumbnails should be produced and stored.
- Design quick save / quick load.
- Design auto save.
- Design an IndexedDB storage adapter.
- Design cloud save storage adapters.
- Design file save / Electron save flows.
- Propagate an appropriate save/load setup to the `create-tsuzuru` template.
- Decide whether `@tsuzuru/standard-ui-preact` should provide a reusable
  SaveLoadPanel while still leaving behavior and storage to the host app.
- Integrate save/load with backlog, message history, and presentation state.
