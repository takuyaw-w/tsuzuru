# @tsuzuru/theme-classic

Classic CSS variable theme for Tsuzuru standard UI.

## Usage

```ts
import { classicThemeClassName } from "@tsuzuru/theme-classic";
import "@tsuzuru/theme-classic/style.css";
```

Apply `classicThemeClassName` to the same host element that contains the
standard UI.
