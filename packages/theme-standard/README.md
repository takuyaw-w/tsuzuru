# @tsuzuru/theme-standard

Standard CSS variable theme for Tsuzuru standard UI.

## Usage

```ts
import { standardThemeClassName } from "@tsuzuru/theme-standard";
import "@tsuzuru/theme-standard/style.css";
```

Apply `standardThemeClassName` to the same host element that contains the
standard UI.
