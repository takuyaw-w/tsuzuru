# @tsuzuru/theme-minimal

Minimal CSS variable theme for Tsuzuru standard UI.

## Usage

```ts
import { minimalThemeClassName } from "@tsuzuru/theme-minimal";
import "@tsuzuru/theme-minimal/style.css";
```

Apply `minimalThemeClassName` to the same host element that contains the
standard UI.
